<DocConfig>
  {({ name }) => (
    <p>
      In researching {name} TLS, we did some thorough investigation. Here are
      our rough notes if you are interested in diving deeper.
    </p>
  )}
</DocConfig>
