Send a `PING` command to Redis to test your TLS configuration:

<CodeBlock
  language="shell-session"
  copyText="redis-cli --tls --cacert /path/to/ca.crt ping"
>
  {`$ redis-cli --tls --cacert /path/to/ca.crt ping
PONG`}
</CodeBlock>
